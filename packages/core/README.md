# @trestlejs/core

Shared project metadata and deterministic infrastructure used by the TrestleJS
toolchain.

Most applications should use the `trestle` CLI or `create-trestlejs` rather
than depending on this package directly.

See the [TrestleJS repository](https://github.com/gregmushen/trestlejs) for
documentation and source code.
